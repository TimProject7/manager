<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.parker.dao.FAQDao">

	<!-- 01. 게시글 전체 목록 조회 -->
	<select id="faqlistAll" resultType="com.parker.vo.FAQVO">
		SELECT
		faq_no,
		faq_title,
		faq_content,
		faq_writer,
		faq_regdate,
		faq_viewcnt
		FROM
		faq_tb
		ORDER BY
		faq_no desc,
		faq_regdate desc
	</select>
	<!-- 02. 게시글 작성 -->
	<insert id="faqinsert">
		INSERT INTO faq_tb (
		faq_no,
		faq_title,
		faq_content,
		faq_writer,
		admin_number,
		faq_regdate
		
		) VALUES (
		seq_faq_tb.nextval,
		#{faq_title},
		#{faq_content},
		#{faq_writer},
		(    select admin_number from
		admin_tb where admin_id=#{admin_id}),
		sysdate
		)
	</insert>
	<!--03. 게시글 상세보기 조회 -->
	<select id="faqview" resultType="com.parker.vo.FAQVO">
		SELECT * FROM faq_tb
		WHERE faq_no
		= #{faq_no}
	</select>
	<!-- 04. 게시글 조회수 증가처리 -->
	<update id="faqincreaseViewcnt">
		UPDATE faq_tb SET faq_viewcnt = faq_viewcnt + 1
		WHERE
		faq_no = #{faq_no}
	</update>
	<!-- 05. 게시글 수정처리 -->
	<update id="faqupdateArticle">
		UPDATE faq_tb SET
		faq_title = #{faq_title},
		faq_content = #{faq_content}
		WHERE faq_no = #{faq_no}
	</update>
	<!-- 06. 게시글 삭제처리 -->
	<delete id="faqdeleteArticle">
		DELETE FROM faq_tb
		WHERE faq_no = #{faq_no}
	</delete>

</mapper>
